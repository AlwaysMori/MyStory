<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyStory" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorAccent">@color/blue_link</item>
    </style>

    <style name="Theme.MyStory" parent="Base.Theme.MyStory" />

    <style name="Theme.MyStory.ActionBar" parent="Theme.Material3.DayNight" >
        <item name="colorAccent">@color/blue_link</item>
    </style>

    <style name="Theme.MyStory.Fullscreen" parent="Theme.MyStory">
        <item name="android:actionBarStyle">@style/Widget.Theme.MyStory.ActionBar.Fullscreen</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.MyStory.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>

</resources>